@using Microsoft.AspNetCore.Identity

@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post" id="logoutForm" class="navbar-right">
        <ul class="nav navbar-nav navbar-right">
            <li>
                <a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">”·«„ @UserManager.GetUserName(User)!</a>
            </li>
            <li>
                <button type="submit" class="btn btn-link navbar-btn navbar-link">Œ—ÊÃ</button>
            </li>
        </ul>
    </form>
}
else
{
    <li class="nav-item pl-5">
        <a asp-area="Identity" asp-page="/Account/Register" class="nav-link text-secondary">
            <i class="fas fa-user-plus align-middle"></i>
            À»  ‰«„
        </a>
    </li>
    <li><hr class="my-0 bg-light" /></li>
    <li class="nav-item pl-5">
        <a asp-area="Identity" asp-page="/Account/Login" class="nav-link text-secondary">
            <i class="fas fa-sign-in-alt align-middle"></i>
            Ê—Êœ
        </a>
    </li>
}